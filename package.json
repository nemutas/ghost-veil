{
	"name": "ghost-veil",
	"homepage": "https://nemutas.github.io/ghost-veil/",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"start": "vite",
		"build": "tsc && vite build --emptyOutDir",
		"preview": "vite preview",
		"deploy": "npm run build && gh-pages -d dist"
	},
	"devDependencies": {
		"gh-pages": "^4.0.0",
		"sass": "^1.53.0",
		"typescript": "^4.5.4",
		"vite": "^2.9.9"
	},
	"dependencies": {
		"ress": "^5.0.2"
	}
}
